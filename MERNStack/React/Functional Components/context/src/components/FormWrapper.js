import React from 'react';
import Form from "./Form";

//This Wrapper Just contains a Form to demo that use context can be used anywhere

const FormWrapper = (props) => {
    return (
        <Form/>
    )

}

export default FormWrapper;